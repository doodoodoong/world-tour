import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `'GangwonEduPowerExtraBoldA', sans-serif`,
    body: `'GangwonEdu_OTFBoldA', sans-serif`,
  },
});

export default theme;
